# ══════════════════════════════════════════════════════════════════════
# Event Lead Router — environment template
# Copy to .env.local for local dev; set the same keys in Vercel for deploys.
# Every value here is required unless marked optional. Nothing in this file
# is a real credential.
# ══════════════════════════════════════════════════════════════════════

# ── Clay ────────────────────────────────────────────────────────────────
# Server-side only. Never referenced from client code, never logged, never
# returned in an error. Generate in Clay: app.clay.com → Settings →
# Account → "API keys (beta)".
CLAY_API_KEY=
CLAY_API_BASE_URL=https://api.clay.com/public/v0

# The Clay Routine that enriches one lead and writes it to the Salesforce
# campaign. Format: function:t_...  (app.clay.com/functions → your
# function → Details → enable API → copy the id). Contract:
# docs/technical-spec.md §3 and docs/routine-contract.md.
CLAY_ROUTINE_ID=

# Optional but recommended: a second, read-only routine backing the campaign
# combobox (name search, member counts, available dispositions). Without it,
# set SALESFORCE_CAMPAIGN_PROVIDER=id-only and paste campaign ids instead.
CLAY_CAMPAIGN_ROUTINE_ID=

# Optional — webhooks are a latency optimization, never a requirement;
# polling is the source of truth. One-time setup:
#   clay webhooks create https://<your-domain>/api/webhooks/clay
# The signingSecret is returned ONCE — capture it immediately.
CLAY_WEBHOOK_ID=
CLAY_WEBHOOK_SIGNING_SECRET=

# ── Salesforce (links only — the Routine owns all writes) ───────────────
# Used to build deep links to Contacts/CampaignMembers in the results.
SALESFORCE_INSTANCE_URL=
# id-only ships as default: campaign ids are format-validated and the
# Routine's write is the real validation (spec §7).
SALESFORCE_CAMPAIGN_PROVIDER=id-only

# ── Supabase ────────────────────────────────────────────────────────────
# Project URL and keys: Supabase dashboard → Project settings → API keys.
NEXT_PUBLIC_SUPABASE_URL=
# The publishable key (sb_publishable_…) — safe for the browser; all client
# access is constrained by Row Level Security. Legacy projects can set
# NEXT_PUBLIC_SUPABASE_ANON_KEY instead; both are honored.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# The secret key (sb_secret_…) — SERVER ONLY. Bypasses RLS; used by route
# handlers, webhook processing, and cron reconciliation. Never expose to
# the client. Legacy alternative: SUPABASE_SERVICE_ROLE_KEY.
SUPABASE_SECRET_KEY=

# ── One-time setup only (scripts/configure-supabase-auth.mjs) ───────────
# Not needed at app runtime. The script wires the Google provider, the
# before-user-created hook, redirect URLs, and syncs the domain allowlist
# into Postgres. SUPABASE_ACCESS_TOKEN comes from `npx supabase login`.
#SUPABASE_PROJECT_REF=
#SITE_URL=
#GOOGLE_CLIENT_ID=
#GOOGLE_CLIENT_SECRET=

# ── Auth / access control ───────────────────────────────────────────────
# Comma-separated list of email domains allowed to sign in, matched exactly
# against the substring after the final @ (lowercased). Empty or unset means
# NOBODY can sign in — the app fails closed and surfaces a setup error.
#   Example: clay.com
#   Example: clay.com,claylabs.com
ALLOWED_EMAIL_DOMAINS=

# ── Vercel Cron ─────────────────────────────────────────────────────────
# Bearer secret protecting /api/cron/* (cron requests carry no session).
# Generate: openssl rand -hex 32
CRON_SECRET=

# ── Tuning (all optional — sane defaults in code) ───────────────────────
# Rows per inline routine run. Clay's hard max is 100.
CHUNK_SIZE=100

# Max chunks in flight against Clay at once. Clay publishes no rate limits;
# 5 is a guess to tune against observed 429s.
MAX_CONCURRENT_CHUNKS=5

# Files with more effective rows than this run as one Clay batch instead of
# chunked inline runs. Lowering it degrades the app toward pure batch mode —
# a config change, not a rewrite.
MAX_INLINE_TOTAL_ROWS=5000

# Hard per-file row ceiling (the "split the file" screen). 50,000 is Clay's
# documented-in-prose batch ceiling — an inference, not a schema constant.
MAX_BATCH_ROWS=50000

# Results page size against Clay. 100 is Clay's max; the default 20 costs
# 5× the reads on the hottest path.
RESULTS_PAGE_LIMIT=100

# Server-side Clay poll coalescing interval (ms) and stall/expiry windows.
POLL_INTERVAL_MS=3000
STALL_THRESHOLD_MINUTES=10
MAX_RUN_AGE_HOURS=24

# ── Dev conveniences (optional) ─────────────────────────────────────────
# Set true to expose /dev/components (the component gallery) in a
# production build. It is always available in dev and always noindexed.
NEXT_PUBLIC_ENABLE_DEV_GALLERY=
